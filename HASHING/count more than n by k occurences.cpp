#include<iostream>
#include<bits/stdc++.h>
int count_occurences(int arr[],int n){
    
}
int main(){
    int arr[]={};
    int n=sizeof(arr)/sizeof(arr[0]);
    count_occurences(arr,n);
    return 0;
}